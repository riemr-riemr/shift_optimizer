<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.riemr.shift.infrastructure.mapper.EmployeeMapper">
  <resultMap id="BaseResultMap" type="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    <id column="employee_code" jdbcType="VARCHAR" property="employeeCode" />
    <result column="store_code" jdbcType="VARCHAR" property="storeCode" />
    <result column="employee_name" jdbcType="VARCHAR" property="employeeName" />
    <result column="min_work_minutes_day" jdbcType="INTEGER" property="minWorkMinutesDay" />
    <result column="max_work_minutes_day" jdbcType="INTEGER" property="maxWorkMinutesDay" />
    <result column="min_work_hours_week" jdbcType="INTEGER" property="minWorkHoursWeek" />
    <result column="max_work_hours_week" jdbcType="INTEGER" property="maxWorkHoursWeek" />
    <!-- password_hash / authority_code はMBGエンティティへはマッピングしない（別DTOで取得） -->
    
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    employee_code, store_code, employee_name,
    min_work_minutes_day, max_work_minutes_day,
    min_work_hours_week, max_work_hours_week
  </sql>
  <select id="selectByExample" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.EmployeeExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from public.employee
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from public.employee
    where employee_code = #{employeeCode,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from public.employee
    where employee_code = #{employeeCode,jdbcType=VARCHAR}
  </delete>
  <delete id="deleteByExample" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.EmployeeExample">
    delete from public.employee
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    insert into public.employee (
      employee_code, store_code, employee_name,
      min_work_minutes_day, max_work_minutes_day,
      min_work_hours_week, max_work_hours_week)
    values (
      #{employeeCode,jdbcType=VARCHAR}, #{storeCode,jdbcType=VARCHAR}, #{employeeName,jdbcType=VARCHAR},
      #{minWorkMinutesDay,jdbcType=INTEGER}, #{maxWorkMinutesDay,jdbcType=INTEGER},
      #{minWorkHoursWeek,jdbcType=INTEGER}, #{maxWorkHoursWeek,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    insert into public.employee
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="employeeCode != null">
        employee_code,
      </if>
      <if test="storeCode != null">
        store_code,
      </if>
      <if test="employeeName != null">
        employee_name,
      </if>
      <if test="minWorkMinutesDay != null">
        min_work_minutes_day,
      </if>
      <if test="maxWorkMinutesDay != null">
        max_work_minutes_day,
      </if>
      <if test="minWorkHoursWeek != null">
        min_work_hours_week,
      </if>
      <if test="maxWorkHoursWeek != null">
        max_work_hours_week,
      </if>
      
      
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="employeeCode != null">
        #{employeeCode,jdbcType=VARCHAR},
      </if>
      <if test="storeCode != null">
        #{storeCode,jdbcType=VARCHAR},
      </if>
      <if test="employeeName != null">
        #{employeeName,jdbcType=VARCHAR},
      </if>
      <if test="minWorkMinutesDay != null">
        #{minWorkMinutesDay,jdbcType=INTEGER},
      </if>
      <if test="maxWorkMinutesDay != null">
        #{maxWorkMinutesDay,jdbcType=INTEGER},
      </if>
      <if test="minWorkHoursWeek != null">
        #{minWorkHoursWeek,jdbcType=INTEGER},
      </if>
      <if test="maxWorkHoursWeek != null">
        #{maxWorkHoursWeek,jdbcType=INTEGER},
      </if>
      
      
    </trim>
  </insert>
  <select id="countByExample" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.EmployeeExample" resultType="java.lang.Long">
    select count(*) from public.employee
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update public.employee
    <set>
      <if test="row.employeeCode != null">
        employee_code = #{row.employeeCode,jdbcType=VARCHAR},
      </if>
      <if test="row.storeCode != null">
        store_code = #{row.storeCode,jdbcType=VARCHAR},
      </if>
      <if test="row.employeeName != null">
        employee_name = #{row.employeeName,jdbcType=VARCHAR},
      </if>
      <if test="row.minWorkMinutesDay != null">
        min_work_minutes_day = #{row.minWorkMinutesDay,jdbcType=INTEGER},
      </if>
      <if test="row.maxWorkMinutesDay != null">
        max_work_minutes_day = #{row.maxWorkMinutesDay,jdbcType=INTEGER},
      </if>
      
    </set>
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update public.employee
    set employee_code = #{row.employeeCode,jdbcType=VARCHAR},
      store_code = #{row.storeCode,jdbcType=VARCHAR},
      employee_name = #{row.employeeName,jdbcType=VARCHAR},
      min_work_minutes_day = #{row.minWorkMinutesDay,jdbcType=INTEGER},
      max_work_minutes_day = #{row.maxWorkMinutesDay,jdbcType=INTEGER},

    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    update public.employee
    <set>
      <if test="storeCode != null">
        store_code = #{storeCode,jdbcType=VARCHAR},
      </if>
      <if test="employeeName != null">
        employee_name = #{employeeName,jdbcType=VARCHAR},
      </if>
      <if test="minWorkMinutesDay != null">
        min_work_minutes_day = #{minWorkMinutesDay,jdbcType=INTEGER},
      </if>
      <if test="maxWorkMinutesDay != null">
        max_work_minutes_day = #{maxWorkMinutesDay,jdbcType=INTEGER},
      </if>
      <if test="minWorkHoursWeek != null">
        min_work_hours_week = #{minWorkHoursWeek,jdbcType=INTEGER},
      </if>
      <if test="maxWorkHoursWeek != null">
        max_work_hours_week = #{maxWorkHoursWeek,jdbcType=INTEGER},
      </if>
      
      
    </set>
    where employee_code = #{employeeCode,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    update public.employee
    set store_code = #{storeCode,jdbcType=VARCHAR},
      employee_name = #{employeeName,jdbcType=VARCHAR},
      min_work_minutes_day = #{minWorkMinutesDay,jdbcType=INTEGER},
      max_work_minutes_day = #{maxWorkMinutesDay,jdbcType=INTEGER},
      min_work_hours_week  = #{minWorkHoursWeek,jdbcType=INTEGER},
      max_work_hours_week  = #{maxWorkHoursWeek,jdbcType=INTEGER}
    where employee_code = #{employeeCode,jdbcType=VARCHAR}
  </update>

  <select id="selectAll" resultMap="BaseResultMap">
    SELECT *
      FROM employee
    ORDER BY employee_code
  </select>

  <select id="selectByStoreCode" parameterType="string" resultMap="BaseResultMap">
    SELECT
      employee_code, store_code, employee_name,
      min_work_minutes_day, max_work_minutes_day,
      min_work_hours_week, max_work_hours_week
    FROM public.employee
    WHERE store_code = #{storeCode}
    ORDER BY employee_code
  </select>

  <!-- Auth: username(=employee_code) でログイン用情報を取得 -->
  <resultMap id="AuthUserMap" type="io.github.riemr.shift.infrastructure.security.AuthUser">
    <id     column="employee_code" property="employeeCode" />
    <result column="employee_name" property="employeeName" />
    <result column="store_code"    property="storeCode" />
    <result column="password_hash" property="passwordHash" />
    <result column="authority_code" property="authorityCode" />
  </resultMap>

  <select id="selectAuthByEmployeeCode" parameterType="string" resultMap="AuthUserMap">
    SELECT employee_code, employee_name, store_code, password_hash, authority_code
      FROM public.employee
     WHERE employee_code = #{employeeCode}
     LIMIT 1
  </select>

  <!-- Custom: update auth fields -->
  <update id="updateAuthFields">
    UPDATE public.employee
       SET password_hash = #{passwordHash},
           authority_code = #{authorityCode}
     WHERE employee_code = #{employeeCode}
  </update>

  <!-- custom upsert (excluding auth fields) -->
  <insert id="upsert" parameterType="io.github.riemr.shift.infrastructure.persistence.entity.Employee">
    INSERT INTO public.employee (
      employee_code, store_code, employee_name,
      min_work_minutes_day, max_work_minutes_day,
      min_work_hours_week, max_work_hours_week)
    VALUES (
      #{employeeCode}, #{storeCode}, #{employeeName},
      #{minWorkMinutesDay}, #{maxWorkMinutesDay},
      #{minWorkHoursWeek}, #{maxWorkHoursWeek})
    ON CONFLICT (employee_code) DO UPDATE SET
      store_code            = EXCLUDED.store_code,
      employee_name         = EXCLUDED.employee_name,
      min_work_hours_week   = EXCLUDED.min_work_hours_week,
      max_work_hours_week   = EXCLUDED.max_work_hours_week,
      min_work_minutes_day  = EXCLUDED.min_work_minutes_day,
      max_work_minutes_day  = EXCLUDED.max_work_minutes_day
  </insert>
</mapper>
